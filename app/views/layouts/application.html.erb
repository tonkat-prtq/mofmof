<!DOCTYPE html>
<html>
  <head>
    <title>DevExam</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expland-md navbar-light bg-light">
      <div class="navbar-brand">
        <%= link_to "mofmof", houses_path, class: "text-decoration-none text-reset" %>
      </div>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item" data-turbolinks="false"><%= link_to "新規作成", new_house_path, class: 'nav-link' %></li>
      </ul>
    </nav>
    <div class="container-fluid">
      <% flash.each do |key, value| %>
        <div class="alert">
          <%= content_tag(:p, value, class: "alert alert-#{key}") %>
        </div>
      <% end %>
    <%= yield %>
  </body>
</html>
